# A door sticker QR encodes a Location URL, not an Access Code

A printed sticker on a door outlives Rotation. The QR therefore encodes `/?location=<Location id>` — the public Visit page with that Location focused. The Visitor still records a Visit before seeing the Access Code. Encoding the four digits would make every poster expire on the next Rotation; skipping the Visit would empty the book.
